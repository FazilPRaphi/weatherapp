// Gradient definitions for energising buttons
import { colors } from './colors';

export const gradients = {
  button: {
    colors: [colors.primary.darkBlue, colors.primary.lightBlue],
    start: { x: 0, y: 0 },
    end: { x: 1, y: 1 },
  },
};
